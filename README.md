# SharpFade Barbershop (No Tailwind, No TypeScript)

- Next.js App Router
- Plain CSS (globals.css)
- Client components where state/handlers are used
- Booking form uses simple `onSubmit` with `preventDefault()`

## Run
npm install
npm run dev
